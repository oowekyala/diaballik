<UserControl x:Class="DiaballikWPF.View.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiaballikWPF.View"
             xmlns:core="clr-namespace:Diaballik.Core;assembly=DiaballikCore"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance core:BoardLike}">
    <Grid>
        <UniformGrid Name="BoardGrid" Columns="{Binding BoardSize}" Rows="{Binding BoardSize}" Height="300" Width="300" />
    </Grid>
</UserControl>



